domain: koo.surge.sh

surge:
  enabled: true

rsync:
  enabled:          false
  username:         remoteuser
  destination:      /home/remoteuser/www/project
  incremental:      true
  progress:         true
  relative:         true
  emptyDirectories: true
  recursive:        true
  clean:            true
  exclude:
    - .DS_Store
    - .git
  include: []
